---
"hl-terminal": minor
---

Update incur to 0.3.13 — adds multiple output formats (toon, json, yaml, md, jsonl), output filtering, JSON Schema export, token management, shell completions, and built-in `mcp add` / `skills add` commands. Documentation updated with missing candles command, HIP-3 DEX market support, and new global options.
